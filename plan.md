# 給与計算実務能力検定2級 合格伴走アプリ「きゅーよ！」設計プラン

## Context

ユーザー（エンジニア）が **2026年11月22日実施の給与計算実務能力検定2級** に合格するため、Duolingo型のゲーミフィケーションで毎日コツコツ学べるWebアプリを新規開発する。[deep-research-report.md](deep-research-report.md) に模試48問（解答・解説付き）・出題範囲・分野別配点・学習タイムラインが揃っており、これをコンテンツと設計の一次資料とする。

**確定した方針（ユーザー回答）:**
- コースは **2級のみに集中**（1級は合格後の拡張。DBはcourse単位で設計し拡張可能に）
- **Laravel 13 + Inertia.js 3 + Vue 3**（TypeScript + Tailwind CSS 4。実装時点の互換バージョンへ更新）
- **マルチユーザー前提、Google認証（Socialite）**。横展開を想定
- 問題コンテンツは **レポート48問シード + オリジナル問題を数百問生成 + 管理画面/インポートも用意**

**注意事項（レポートより）:** 公式は現行過去問を非公開。アプリ内の問題は公式出題範囲・形式に準拠したオリジナル問題とし、料率・税額は2026年度の一次資料値（レポート記載: 健保9.85%、介護込11.47%、子育て支援金0.23%、厚年18.3%、雇用保険労働者負担5/1000等）を使用する。

## 技術スタック

| レイヤ          | 選定                                                                                                                    |
| --------------- | ----------------------------------------------------------------------------------------------------------------------- |
| バックエンド    | Laravel 13（PHP 8.4）                                                                                                   |
| フロント        | Inertia.js 3 + Vue 3 + TypeScript + Tailwind CSS 4 + Vite                                                               |
| DB / キャッシュ | PostgreSQL 17 / Redis（キュー・キャッシュ・セッション）                                                                 |
| 認証            | Laravel Socialite（Google OAuth）                                                                                       |
| 管理画面        | Filament 5（`/admin`、Inertia本体とは独立）                                                                             |
| テスト          | Pest                                                                                                                    |
| Docker          | docker compose: `app`(FrankenPHP) / `postgres` / `redis` / `mailpit` / `vite`(node)。本番用マルチステージDockerfile同梱 |

FrankenPHP単一イメージでdev/prod共通化（nginx不要でモダン・シンプル）。scheduler/queueはdevではcompose別サービス、本番はsupervisordで `schedule:work` / `queue:work` を起動。リポジトリは `git init` から開始。

**スケジューラの仕事**：デイリークエスト生成（毎日0時JST）／ストリーク切れ判定＆フリーズ自動消費（毎日0時JST）／週間リーグ集計・リセット（月曜0時JST）。リマインダーメールは設定・送信ロジックまで実装済みだが、本番メール配送はバックログとする。バッジ判定・リーグXP加算はキュー（Redis）で非同期。

**認証**：Google OAuth（Socialite）。ただしローカル開発ではGCP設定なしで動くよう、`local` 環境限定のdevログイン（ワンクリックでテストユーザーにログイン）を用意。

## アプリ設計

### 学習構造（Duolingoのスキルツリー相当）
```
Course(2級) → Unit(分野) → Lesson(5〜8問) → Question
```
Unitはレポートの分野構成に準拠：
1. 労働法・勤怠（労基法24/32/35/37/39条、割増、年休、就業規則）
2. 給与基礎・支給控除（通勤費非課税、給与明細、支給/控除項目）
3. 税（源泉所得税・月額表・賞与算出率表・住民税特別徴収）
4. 社会保険（標準報酬、定時決定・随時改定、料率、入退社、賞与）
5. 実務計算（割増賃金・保険料・源泉税・手取りの複合計算）
6. 発展：年末調整（1級範囲だが橋渡し用、レポートQ41-48）

各Lessonは クラウン（習熟度0〜5）を持ち、繰り返すほど上がる。Unit内は順にアンロック。

### 問題タイプ
- **四肢択一**（メイン。本番形式）
- **計算問題**：アプリ内「資料集」（2026年度 協会けんぽ料額表・月額表抜粋・賞与算出率表・雇用保険料率・通勤費非課税限度額）をモーダル参照しながら解く。本番の「表を正しく引く技能」を再現
- **数値入力**（練習用。計算過程の途中値を答える）

`questions` はJSONBで choices / answer / explanation / 典型ミス（レポートの「頻出ミス」を格納）/ 難易度 / 分野タグを持ち、全タイプを1テーブルで表現。数値入力問題はカンマ・全角数字を正規化して判定。

**解答判定はすべてサーバー側**：問題配信APIは正解・解説を含めず、解答送信APIが判定→正誤・解説・XPを返す。リーグ（対人ランキング）があるため正解のフロント漏洩・XPチートを構造的に防ぐ。判定と同時に `question_attempts` 記録・SRS登録・クエスト進捗・XP付与を1トランザクションで処理。

### ゲーミフィケーション
- **XP・デイリーゴール**：正解でXP（難易度別 10/15/20）、コンボボーナス。ゴールは10/20/30/50 XPから選択
- **ストリーク**：ゴール達成日の連続記録。炎アイコン、フリーズアイテム（週1自動付与）。深夜0時境界、Asia/Tokyo
- **SRS復習キュー**：誤答は自動でLeitner方式（間隔 1→3→7→14→30日）の復習箱へ。「復習」タブに今日の復習問題が並ぶ
- **デイリークエスト**：毎日3つ自動生成（例「計算問題を3問正解」「復習を5問クリア」）→ 追加XP
- **バッジ**：ストリーク7/30/100日、分野マスター、模試70点/80点/90点、など
- **週間リーグ**：週間XPランキング（マルチユーザーなので対人要素あり。月曜リセット、Redisで集計）
- **学習ヒートマップ** + **試験日カウントダウン**（2026-11-22まで残りN日を常時表示）
- **リマインダー**：ON/OFF・時刻設定と未達成判定・通知生成まで実装済み。devはMailpitで確認できる。本番メール配送はプロバイダー・送信ドメインを決めるまでバックログ
- **マスコット「きゅーちゃん」**：SVG自作のかわいいキャラ（給与袋モチーフ）。正解で喜ぶ／誤答で励ます／レッスン完了で紙吹雪＋ファンファーレ。効果音はWeb Audioで軽量に（設定でミュート可）
- **かわいいUI**：パステル系パレット、丸ゴシック（M PLUS Rounded 1c）、角丸カード、マイクロアニメーション（Vue Transition + CSS）。**モバイルファースト**（毎日の学習はスマホ想定・下タブナビ）＋PWA対応（ホーム画面追加・オフラインは静的シェルのみ）
- **オンボーディング**：初回ログイン時にデイリーゴール選択・リマインダー時刻設定・試験日確認（2026-11-22デフォルト、変更可）の3ステップ

### 模試モード（本番耐性）
- レポートQ1〜Q40を完全再現：**40問・120分タイマー・マークシート風UI・電卓可・資料集参照可**
- **途中保存・再開**：解答は随時サーバー保存（Inertia 2のポーリング/部分リロード活用）。ブラウザが落ちても残り時間から再開可能
- 採点は知識2点×35＋計算6点×5=100点、70点合格ライン表示
- 終了後に**分野別診断**（レポートの失点パターン表を実装：誤答分野→優先復習領域を提示し、該当Unitへのリンク）
- 90分圧縮モード、スコア推移グラフ
- 模試は複数セット化（生成コンテンツから40問構成の模試を追加）

### ダッシュボード
- 今日のゴール進捗リング、ストリーク、復習キュー件数、クエスト
- 分野別正答率レーダーチャート、模試スコア推移、**合格予測メーター**（分野別正答率×レポートの配点比率で推定得点を算出）
- レポートの学習タイムライン（8月基礎→9月社保→10月税→模試→直前）を「シーズンマップ」として表示し、今どの期にいるかを可視化

### 管理画面（Filament）
- 問題・レッスン・UnitのCRUD
- **CSV/JSONインポート**（スキーマ定義したJSON形式。バリデーション付き）
- ユーザー・統計閲覧

## データモデル（主要テーブル）

`users`（google_id, is_admin, daily_goal, reminder_time, exam_date）/ `courses` / `units` / `lessons` / `questions`（fiscal_year付き）/ `question_attempts` / `lesson_progresses`（crown_level）/ `user_stats`（total_xp, current_streak, longest_streak, last_active_date, streak_freezes）/ `daily_activities`（date, xp — ヒートマップ・ストリーク判定）/ `review_items`（question_id, box, due_date）/ `daily_quests` / `badges` + `user_badges` / `league_scores`（week, xp）/ `mock_exams` + `mock_exam_questions` + `mock_exam_attempts`（answers JSONB, started_at, 分野別スコア — 途中保存兼用）/ `reference_sheets`（資料集データ）

**料率の年度バージョニング**：`reference_sheets` と `questions` に `fiscal_year`（2026年度＝法令基準日2026-09-01）を持たせる。計算問題は料率をハードコードせず問題データに使用値を明記し、翌年度改定時は新年度シートと該当問題の一括差し替えで対応できる構造にする（1級展開・来年度受験者への横展開に効く）。

## コンテンツ計画

1. **シード第1弾**：レポートの48問（Q1-40本番模試 + Q41-48発展）を解説・頻出ミス込みでJSONシーダー化。模試セット1として登録
2. **オリジナル問題生成（実装内で私が執筆）**：出題範囲チェックリストを網羅する **300問** をUnit別に収録（労働法・勤怠 60 / 支給控除 45 / 税 70 / 社会保険 85 / 実務計算 40）。知識260問は31の中核ルールを複数の業務場面で反復する定着問題であり、260個の独立論点という意味ではない。すべて解説・誤答理由付き。料率は2026年度値で統一
3. **自動検算による品質保証**：計算問題はシードJSONに計算パラメータ（基本給・料率・端数処理等）を構造化して持たせ、Pestテストで**正答をPHPで再計算して照合**する。300問規模でも誤答シードを機械的に排除。知識問題はレポート・一次資料値との整合をシーダーバリデーション（正解が選択肢に存在、解説必須等）でチェック
4. 生成問題から**追加模試セット2〜3**を構成
5. 資料集データ（協会けんぽ料額表・月額表抜粋〔模試で使う給与帯域±α〕・賞与算出率表・雇用保険料率・通勤費非課税限度額）をシード。計算問題は参照すべき資料集シートに紐付け

## 実装フェーズ

- [x] **基盤**：`git init`、Laravel 12スキャフォールド（Vueスターターキット）、Docker構成、Socialite Google認証＋devログイン、Pest
- [x] **ドメイン**：マイグレーション・モデル・シーダー（48問＋資料集）＋検算テスト基盤
- [x] **レッスン体験**：スキルツリー画面 → 問題プレイヤー（択一/計算/資料集モーダル、サーバー側判定API）→ 結果画面（XP獲得演出）。ここが体験の核
- [x] **習慣ループ**：XP・ストリーク・デイリーゴール・SRS復習・ヒートマップ・ダッシュボード・オンボーディング・リマインダー設定／送信ロジック
- [x] **模試モード**：120分タイマー・途中保存再開・マークシートUI・分野別診断
- [x] **ゲーミフィケーション拡張**：クエスト・バッジ・週間リーグ・マスコット演出・効果音・PWA
- [x] **管理画面**：Filament + インポート
- [x] **コンテンツ量産**：300問生成（検算テスト通過必須）・模試セット追加・最終シード

各フェーズ完了時に動く状態を保つ（フェーズ3完了時点で「毎日1レッスンやる」最小体験が成立）。

## バックログ

- [ ] 本番メール配送プロバイダーの選定と設定（SES / Resend / SMTP等）
- [ ] 送信ドメインのSPF・DKIM・DMARC設定と到達率確認
- [ ] 学習リマインダー、パスワード再設定、メール確認の本番E2Eテスト
- [ ] バウンス・苦情・配信停止・キュー失敗の監視と運用手順

本番環境はバックログ完了まで `MAIL_MAILER=log` とし、外部へメールを送信しない。

## 検証方法

- `docker compose up -d` → `php artisan migrate --seed` で起動確認
- Pestテスト：XP付与・ストリーク判定（JST日付境界・フリーズ消費含む）・SRS間隔遷移・模試採点（70点判定・分野別集計）・クラウン進行・解答判定API（正解漏洩がないことのレスポンス検証）のユニット/Featureテスト
- **計算問題の全件検算テスト**：シード済み計算問題の正答をPHPで再計算して一致確認（コンテンツ品質ゲート）
- Google認証はSocialite mockでFeatureテスト、devログインで実ブラウザ確認
- ブラウザ（preview）でレッスン1周・復習・模試1回を通しプレイし、演出・採点・診断・モバイル表示（375px）を目視確認
