# 監査済み現行内容の公開記録

2026年9月6日にユーザーから「mainにプルリク、マージもして。mainにマージしたら自動でデプロイされる仕組み」と指示を受けた。9月7日に再開し、個別監査済み567問とアプリ変更の公開工程を進める。

## 公開判断と範囲

開始時831問の全IDと追加6問に判定があり、現行567問には本文・選択肢・解説・根拠・依存資料の個別確認が揃っている。270退役問は配信せず、履歴と理由を保存する。この現行内容の公開を承認し、`release.editorial_status` を `approved` に変更した。各問の承認、指紋一致、期限、根拠確認の仕組みは維持する。

これは公式教材の全範囲・深さ・十分な演習量、模試の実測難易度、実学習者の合格効果の承認ではない。`coverage_status=unverified`、`learner_effectiveness_status=not_measured` をリリースへ明記し、[監査報告の保留事項](README.md)を残す。マージ依頼を根拠に内容の誤りや個別監査漏れを無視する扱いではない。

## 公開前の検証

- 2026年9月7日、実台帳の `content:review-audit --json`：567問、エラー0。
- 新規の隔離SQLiteに実台帳を使ってマイグレーション、`content:sync`、`content:audit --strict` を実行。567問、通常と模試を合わせたコア302問、模試3件、期限切れ0で成功。
- 同じDBで再同期し、最新と判定してDBレビュー情報を保持。架空の承認データはこの検証に使用していない。
- 既存の実DBの再構築は行わない。本番起動時は既存のマイグレーションと通常同期の経路を使い、解答履歴を保持する。

アプリ回帰・整形・型検査・静的解析は `composer ci:check` を再実行し、結果をchecks.jsonへ追記する。GitHubのPR上でもCI成功を確認してからマージする。mainへの反映後にRailwayの対象コミットのデプロイ状態を確認する。
